setwd("C:/Users/Lenovo/OneDrive/Documents/R-for-Data-Science-and-Machine-Learning-with-NBICT-LAB-Batch-8/RDSML-Day-53 👉 Support Vector Regression in R (Part-1)")
#set up package and data loading
install.packages(c("e1071","dplyr"))
library(e1071)
library(ggplot2)
library(dplyr)
#loading the data set
dataset = read.csv("day.csv")
dataset
View(dataset)
# Basic cleaning and features selection
bike = dataset %>%
select(cnt,temp,atemp,hum,windspeed,season,yr,mnth,holiday
,workingday,weathersit)
View(bike)
# Convert categorical variables to factor
bike = bike %>%
mutate(
season = factor(season),
yr = factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
workingday = factor(workingday),
weathersit = factor(weathersit),
)
str(bike)
bike = dataset %>%
select(cnt,temp,atemp,hum,windspeed,season,yr,mnth,holiday
,weekday,workingday,weathersit)
bike = bike %>%
mutate(
season = factor(season),
yr = factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
workingday = factor(workingday),
weathersit = factor(weathersit),
)
str(bike)
bike = dataset %>%
select(cnt,temp,atemp,hum,windspeed,season,yr,mnth,holiday
,weekday,workingday,weathersit)
bike = bike %>%
mutate(
season = factor(season),
yr = factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
weekday = factor(weekday),
workingday = factor(workingday),
weathersit = factor(weathersit),
)
str(bike)
n = nrow(bike)
n
indices = seq_len
View(indices)
train_idx = sample(indices,size=0.7*n)
